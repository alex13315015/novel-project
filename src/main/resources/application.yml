spring:
  datasource:
    url: jdbc:mysql://localhost:3306/novel
    username: root
    password: wls960aha!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true

  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 2000
    redis:
      host: 127.0.0.1
      port: 6379
      timeout: 10
      
      
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace

file:
  dir: "/Users/ijinmo/Desktop/novel-img/"

logging:
  level:
    org.hibernate.sql: trace
    org.hibernate.type: trace

